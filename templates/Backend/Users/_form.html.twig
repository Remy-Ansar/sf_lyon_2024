{{ form_start(form, {'attr': {'class': 'card p-3 w-75 mx-auto'}}) }}


<div class="row">
	<div class="col-md-6">
		{{ form_row(form.name)}}
	</div>
	<div class="col-md-6">
		{{ form_row(form.lastName)}}
	</div>

	{{ form_row(form.email) }}
	{{ form_row(form.roles) }}

</div>

{{ form_end(form) }}
