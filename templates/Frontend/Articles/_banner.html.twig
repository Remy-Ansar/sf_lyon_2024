<section class="banner-hero" style="background: center / cover url('{{ vich_uploader_asset(article) }}')">
	<div class="banner-hero-header">
		<h1 class="banner-hero-title">{{ article.titre }}</h1>
	</div>
</section>
